#include <stdio.h>
#include <stdlib.h>
#include <string.h>

typedef struct
{
    int dia, mes, ano;
} data;

typedef struct
{
    char nome[100];
    data nascimento;
    char genero;
} contato;

int imprimir(contato **c, int qtty);

int main()
{
    contato *agenda[50];
    int tam = 0, qtd = 0;
    system("cls");

    return 0;
}

int imprimir(contato **c, int qtty)
{
printf()

}